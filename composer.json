{
    "name": "labile/astaroth-framework",
    "description": "framework for creating VK bots",
    "type": "project",
    "authors": [
        {
            "name": "labile",
            "email": "1a6i1e@gmail.com"
        }
    ],

    "minimum-stability": "stable",

    "require": {
        "php": ">=8.1",
        "ext-pcntl": "*",
        "ext-curl": "*",
        "ext-mbstring": "*",

        "labile/astaroth-core": "dev-rolling"
    },

    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },

    "scripts": {
        "serve": [
            "Composer\\Config::disableProcessTimeout",
            "php -S 0.0.0.0:8000"
        ]
    }
}
